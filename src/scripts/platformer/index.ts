/**
 * Page Platformer Easter Egg
 * 
 * A small platformer game that lets users jump around on page elements.
 * Only activates when the user clicks on the avatar.
 */

export { initPlatformer, getEngine, PlatformerEngine } from './engine';
export type * from './types';
